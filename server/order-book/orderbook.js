import Promise from 'bluebird';

export default class orderbook {
  constructor(options) {
    this.bids = [];
    this.asks = [];
  }

  submitBuy() {
    bids.push(10)
  }

  submitSell() {
    asks.push(10)
  }

  
}
